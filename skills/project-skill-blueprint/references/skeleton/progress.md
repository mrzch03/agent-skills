# 进度(轻账本:只存 git 表达不了的东西)

> 进度的主账本是 git。这里只记 git 看不出来的:**待验证行为** 和 **已排除的假设**。

## 待验证行为(代码已合,真环境没证过)
- **<行为措辞,不写 commit/版本号>**:<期望在真环境看到什么跳变 + 怎么确认>。验过即删行。

## 已排除假设(走过的弯路,别重走)
- <假设> —— 被 <什么证据> 推翻。<可泛化的提炼进 lessons/heuristics.md>。
